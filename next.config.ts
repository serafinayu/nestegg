import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */

  /*
    To download images from an external source, you have to allow different hosts

    images: {
      remotePatterns:: [{hostname: "upload.wikipedia.org"}],
    },
  */
};

export default nextConfig;
